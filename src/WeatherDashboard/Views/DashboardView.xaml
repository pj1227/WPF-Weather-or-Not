<UserControl x:Class="WeatherDashboard.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:WeatherDashboard.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <converters:TempUnitConverter x:Key="TempUnitConverter"/>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980b9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#95a5a6"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <Border Grid.Row="0" Background="White" Padding="20" BorderBrush="#ECF0F1" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Search Controls (Row 0) -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource TextBoxStyle}"
                     VerticalAlignment="Center"
                     Margin="0,0,10,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchLocationCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <Button Grid.Column="1" 
                    Content="Search Location" 
                    Command="{Binding SearchLocationCommand}"
                    Style="{StaticResource ButtonStyle}"
                    Margin="0,0,10,0"/>

                    <ComboBox Grid.Column="2"
                      ItemsSource="{Binding Locations}"
                      SelectedItem="{Binding SelectedLocation}"
                      DisplayMemberPath="Name"
                      Width="200"
                      Height="38"
                      FontSize="14"
                      Margin="0,0,10,0"
                      VerticalContentAlignment="Center"/>

                    <Button Grid.Column="3" 
                    Content="Refresh" 
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource ButtonStyle}"/>
                </Grid>

                <!-- Settings Controls (Row 1) - NEW SECTION -->
                <Grid Grid.Row="1" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Temperature Unit Toggle -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                        <TextBlock Text="Units:" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"
                           FontWeight="SemiBold"/>
                        <Button Content="{Binding UseCelsius, Converter={StaticResource TempUnitConverter}}"
                        Command="{Binding ToggleTemperatureUnitCommand}"
                        Width="80"
                        Height="32"
                        Background="#9b59b6"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        FontSize="14">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4"
                                    Padding="10,5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#8e44ad"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>

                    <!-- Set Default Location -->
                    <Button Grid.Column="1"
                    Content="Set as Default Location"
                    Command="{Binding SetDefaultLocationCommand}"
                    Padding="15,8"
                    Background="#27ae60"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontSize="13"
                    IsEnabled="{Binding SelectedLocation, Converter={StaticResource NullToBoolConverter}}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#229954"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#95a5a6"/>
                                        <Setter Property="Cursor" Value="Arrow"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- Current Weather Card -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentWeather.LocationName, FallbackValue='No Location'}" 
                                   FontSize="28" 
                                   FontWeight="Bold"
                                   TextAlignment="Center"/>

                        <TextBlock Text="{Binding CurrentWeather.Country, FallbackValue=''}" 
                                   FontSize="16" 
                                   Foreground="Gray"
                                   TextAlignment="Center"
                                   Margin="0,5,0,0"/>

                        <TextBlock Text="{Binding LastUpdated, StringFormat='Last updated: {0:hh:mm tt}', FallbackValue=''}" 
                                   FontSize="12" 
                                   Foreground="Gray"
                                   TextAlignment="Center"
                                   Margin="0,5,0,30"/>

                        <TextBlock Text="{Binding FormattedTemperature, FallbackValue='--°'}" 
                                   FontSize="72" 
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Foreground="#3498db"/>

                        <TextBlock Text="{Binding CurrentWeather.Description, FallbackValue='No data'}" 
                                   FontSize="20" 
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,30"/>

                        <!-- Weather Details -->
                        <Grid Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Feels Like" Foreground="Gray"/>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding FormattedFeelsLike, FallbackValue='--°'}" 
                                       HorizontalAlignment="Right" 
                                       FontWeight="SemiBold"/>
                        </Grid>

                        <Grid Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Humidity" Foreground="Gray"/>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding CurrentWeather.Humidity, StringFormat='{}{0:F0}%', FallbackValue='--'}" 
                                       HorizontalAlignment="Right" 
                                       FontWeight="SemiBold"/>
                        </Grid>

                        <Grid Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Wind Speed" Foreground="Gray"/>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding CurrentWeather.WindSpeed, StringFormat='{}{0:F1} m/s', FallbackValue='--'}" 
                                       HorizontalAlignment="Right" 
                                       FontWeight="SemiBold"/>
                        </Grid>

                        <Grid Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Pressure" Foreground="Gray"/>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding CurrentWeather.Pressure, StringFormat='{}{0:F0} hPa', FallbackValue='--'}" 
                                       HorizontalAlignment="Right" 
                                       FontWeight="SemiBold"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Forecast Card -->
                <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="5-Day Forecast" 
                                   FontSize="24" 
                                   FontWeight="Bold"
                                   Margin="0,0,0,20"/>

                        <ItemsControl ItemsSource="{Binding Forecast}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8F9FA" 
                                            CornerRadius="8" 
                                            Padding="15" 
                                            Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="3*"/>
                                                <ColumnDefinition Width="2*"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" 
                                                       Text="{Binding Date, StringFormat='{}{0:ddd, MMM d}'}"
                                                       VerticalAlignment="Center"
                                                       FontWeight="SemiBold"/>

                                            <TextBlock Grid.Column="1" 
                                                       Text="{Binding Description}"
                                                       VerticalAlignment="Center"
                                                       TextWrapping="Wrap"
                                                       Margin="10,0"/>

                                            <StackPanel Grid.Column="2" 
                                                        Orientation="Horizontal" 
                                                        HorizontalAlignment="Right"
                                                        VerticalAlignment="Center">
                                                <TextBlock Text="{Binding TempMax, StringFormat='{}{0:F0}°'}" 
                                                           FontWeight="Bold"
                                                           FontSize="16"/>
                                                <TextBlock Text=" / " 
                                                           Foreground="Gray"
                                                           Margin="5,0"/>
                                                <TextBlock Text="{Binding TempMin, StringFormat='{}{0:F0}°'}" 
                                                           Foreground="Gray"
                                                           FontSize="16"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="2" 
              Background="#80000000" 
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                             Width="200" 
                             Height="4"
                             Margin="0,0,0,15"/>
                <TextBlock Text="Loading..." 
                           Foreground="White" 
                           FontSize="16"/>
            </StackPanel>
        </Grid>

        <!-- Error Message -->
        <Border Grid.Row="1"
                Background="#E74C3C" 
                Padding="15"
                VerticalAlignment="Top"
                Margin="20,20,20,0"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠ " 
                           Foreground="White" 
                           FontSize="16" 
                           FontWeight="Bold"/>
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="White" 
                           FontSize="14"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>